PRINTSTR MACRO STR
LEA DX,STR
MOV AH,09H
INT 21H
ENDM

SCANSTR MACRO STR
LEA DX,STR
MOV AH,3FH
INT 21H
ENDM

FINISH MACRO
MOV AH,4CH
INT 21H
ENDM

.MODEL SMALL
.DATA
	MSG	DB	'12345',10,13,'$'
	MSG1	DB	'ENTER STRING PLEASE: ',10,13,'$'
	MSG2	DB	'THE REVERSE IS AS FOLLOWS',10,13,'$'
	STR1	DB	'',10,13
	STR2	DB	'',10,13
.CODE
	MOV AX,@DATA
	MOV DS,AX
	
	PRINTSTR MSG1
	
	SCANSTR STR1
	
	XOR CX,CX
	XOR SI,SI
	XOR BL,BL
	LEA SI,STR1
	
	L1:
		MOV DX,[SI]
		MOV CX,DX
		INC BL
		INC SI
		XOR CH,CH
		CMP CX,0024H;'$'
		JNE L1
	SUB BL,1
	ADD BL,30H

;	MOV DL,BL
;	MOV AH,02H
;	INT 21H

	PRINTSTR MSG2
	
	
	XOR AX,AX
	XOR DX,DX
	XOR CX,CX
	MOV CL,BL
;	LEA SI,STR1
;	ADD SI,CL
	L2:
		DEC SI
		MOV DL,[SI]
		MOV AH,02H
		INT 21H
		DEC CL
		JNZ L2

	FINISH
END
